# Runtime dependencies for the vendor_manager MCP server (P11.T2).
#
# Installed inside the MCP Docker image (see Dockerfile.mcp) and by developers
# who want to run the MCP server locally against a dev vendor_manager instance.
#
# NOT installed in the main app image — the MCP server does not import Django
# and vendor_manager does not import fastmcp.
#
# Install with:  pip install -r requirements-mcp.txt

# --- MCP server framework ---
fastmcp>=3.0,<4.0

# --- HTTP client used to reach the vendor_manager REST API ---
httpx>=0.27,<1.0

# --- Data validation (already a fastmcp dep, pinned here for clarity) ---
pydantic>=2,<3

# --- Structured logging in-cluster ---
python-json-logger>=2.0,<4.0
