# Development-only dependencies.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
#
# Do NOT install these in the production Docker image.

-r requirements.txt

# --- Lint / format / type-check ---
ruff>=0.7,<1.0
mypy>=1.13,<2.0
django-stubs[compatible-mypy]>=5.1,<6.0
djangorestframework-stubs[compatible-mypy]>=3.15,<4.0

# --- Test ---
pytest>=8.3,<9.0
pytest-django>=4.9,<5.0
pytest-cov>=6.0,<8.0
coverage[toml]>=7.6,<8.0
diff-cover>=9.2,<11.0
factory-boy>=3.3,<4.0
faker>=30.0,<40.0
freezegun>=1.5,<2.0

# --- Docs (Phase 6) ---
-r requirements-docs.txt

# --- Dev workflow ---
pre-commit>=4.0,<5.0
django-upgrade>=1.22,<2.0
ipykernel

# --- Optional convenience for hitting the API from scripts (P3.T6) ---
requests>=2.32,<3.0
